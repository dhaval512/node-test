pipeline{
    agent{
        label'master'
    }
    stages{
        stage('docker file for stage'){
            steps{
                sh '''
                echo "hello"
                '''
                 }
            }
        }
}
